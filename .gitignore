.build/
.swiftpm/
Zielzeit.app/
*.xcodeproj
.DS_Store

dist/

# Copied from docs/ by `make site` and by the Pages workflow, never committed.
site/img/
# Written by Scripts/usage-badges during the Pages build. The sample history it
# needs lives on the published site, not in git, so nothing here is a source.
site/badge/
.playwright-mcp/

# Working notes from the Superpowers workflow: design specs, implementation
# plans and the execution ledger. Local only. This repo is public and these are
# scratch, not documentation — CONTRIBUTING and the code comments carry anything worth
# keeping. They were committed once and scrubbed from history; keep them out.
docs/superpowers/
.superpowers/

# The long-form engineering log. Local only now: it was committed for a while,
# so it is still in history, but it is no longer part of the published repo.
CLAUDE.md

# Assistant configuration — settings, agents, skills. Local only, like CLAUDE.md
# above: it is tooling for whoever is working on the repo, not part of the
# product, and CONTRIBUTING carries anything about the workflow worth publishing.
.claude/
